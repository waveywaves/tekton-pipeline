apiVersion: tekton.dev/v1
kind: PipelineRun
metadata:
  name: clone-and-list-run
spec:
  pipelineRef:
    name: clone-and-list-pipeline
  workspaces:
    - name: shared-data
      volumeClaimTemplate:
        spec:
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: 1Gi
